// Scroll-to-top during navigation is now handled by the JS transition hooks
// in app/layouts/default.vue (onLeave smooth-scrolls while blurring).
// This plugin is intentionally empty.
export default defineNuxtPlugin(() => {})
