# Optional overrides for local dev / deploy (copy to `.env` — never commit real secrets).

# Contact form → Resend (`server/api/contact.post.ts`)
NUXT_RESEND_API_KEY=
NUXT_CONTACT_TO_EMAIL=
NUXT_CONTACT_FROM_EMAIL=

# Site URL (also used for canonical URLs)
# NUXT_PUBLIC_SITE_URL=https://bryanxmendez.com
